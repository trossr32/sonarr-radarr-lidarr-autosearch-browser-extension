name: Outdated package checks

# Run workflow on pull request to the main branch
on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [ master, main, staging ]
    paths:
      - '.github/workflows/outdated.yml'
      - 'src/**'
      - 'tests/**'
      - 'package.json'
      - 'tailwind.config.js'
  
  workflow_dispatch:

jobs:
  outdated-packages-check:
    runs-on: ubuntu-slim

    name: Check outdated packages (${{ matrix.name }})

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: project root
            directory: '.'
          - name: playwright tests
            directory: 'tests/playwright'

    steps:
      - name: Check for outdated npm packages in ${{ matrix.name }}
        uses: trossr32/outdated-packages-action@bec045471fbe8558e314b4b41b76eaff9ed6b104
        with:
          use-npm-outdated: true
          npm-project-directory: ${{ matrix.directory }}
          comment-label: '${{ matrix.name }}'
